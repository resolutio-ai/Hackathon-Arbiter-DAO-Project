version: "3"

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: resolution-frontend
    restart: always
    volumes:
      - ./:/app
      - /app/node_modules
      - /app/.next
    ports:
      - 3000:3000
